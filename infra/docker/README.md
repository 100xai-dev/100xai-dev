# Docker Notes

`docker-compose.yml` at the repo root starts only shared local services for now:

- Postgres
- Redis

Application containers should be added when the API and worker dependency strategy is finalized.
